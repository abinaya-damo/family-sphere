# Family Sphere v271 — Tree no snap-back after drag/zoom

Built from v270.

Fixes:
- After zooming and dragging/panning the Family Tree, releasing the mouse/finger no longer resets the tree to the normal fitted state.
- The exact tree viewport is preserved.
- Going to another page and returning to Family Tree keeps/restores the same view.
- Automatic fit is still used only when needed for a fresh view or changed family structure.

All other flows remain the same.
