# Family Sphere v262 — clean app launch

Built directly from v261.

Changed only:
- Removed the literal `\n` text that was visible before Family Sphere finished loading.
- Disabled the Next.js development `N` indicator shown at the bottom-left during `next dev`.

Strictly unchanged:
- v259 notification/reminder backend and behavior
- v261 universal multi-browser live sync
- persistent login / refresh behavior
- Family Tree, Document Vault, Help Board, Events, dashboard and all UI/workflows

No new Supabase SQL is required.
